# Pomodoro API (Server)

## Setup
- Create a Postgres database and set `DATABASE_URL`.
- Copy `.env.example` to `.env` and fill values.

## Run
```bash
npm run dev
```

## Database
Run the SQL files:
- `sql/schema.sql`
- `sql/seed.sql`

